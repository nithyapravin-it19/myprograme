#include <stdio.h>
void main() {
    int a,b,p;
    printf("enter to if it is a leap year");
    scanf("%d",&a);
    if(a%4==0)
    {
    printf("leap year");
    }
    else
    {
    printf("non leap year");
    }
    
}
